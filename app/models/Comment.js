function Comment(author, comment, idPost, createdDate, invisible) {
    this.author = author;
    this.comment = comment;
    this.idPost = idPost;
    this.createdDate = createdDate;
    this.invisible = invisible;
}

